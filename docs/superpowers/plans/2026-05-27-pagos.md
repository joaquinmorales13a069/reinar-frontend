# Rama 13 — Pagos: Plan de Implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Habilitar el módulo Pagos en el ERP: un endpoint global nuevo en el backend (`GET /api/v1/pagos`) + dos páginas frontend (`/pagos` listado y `/pagos/nuevo` registro), reutilizando el form de registro existente.

**Architecture:** PR1 en el server agrega `listarPagosGlobal` (sin migración Prisma). PR2 en el frontend (`feat/pagos`) consume ese endpoint con un nuevo hook React Query, monta dos páginas y mueve `RegistrarPagoForm` a `components/pagos/` para reutilizarlo desde el detalle de factura y desde la página standalone. Se respeta la convención del proyecto: toasts en mutations, errores 422-detalles inline con `setError`, comentarios "why" en español.

**Tech Stack:** Backend Express + Prisma + Zod. Frontend Next.js 19 App Router, TanStack Query v5, RHF + Zod, Tailwind v4, sonner, decimal.js.

**Spec:** `docs/superpowers/specs/2026-05-27-pagos-design.md`

**Pre-requisitos:**
- Rama frontend `feat/pagos` ya creada desde `main` actualizado (✓ hecho en brainstorming).
- En el backend trabajar sobre rama nueva `feat/pagos-listado-global` desde `main` del repo `/Users/joaquinmorales13a06/Desktop/Reinar/server`. Mergear ese PR **antes** de mergear el del frontend.
- No hay suite de tests en este proyecto. La verificación primaria es `pnpm tsc --noEmit` (frontend) y `pnpm tsc --noEmit` o `pnpm build` (backend), más smoke tests manuales con `curl`/`pnpm dev`.

---

## File Structure

### Backend (`/Users/joaquinmorales13a06/Desktop/Reinar/server`, rama `feat/pagos-listado-global`)

| Archivo | Acción | Responsabilidad |
|---|---|---|
| `src/modules/pagos/pagos.schemas.ts` | Modificar | Agregar `listarPagosGlobalQuery` con todos los filtros |
| `src/modules/pagos/pagos.service.ts` | Modificar | Agregar `listarPagosGlobal(filtros)` |
| `src/modules/pagos/pagos.controller.ts` | **Crear** | Controller con `listarPagosGlobal` |
| `src/modules/pagos/pagos.routes.ts` | **Crear** | Router con `GET /` |
| `src/index.ts` | Modificar | Importar y montar `pagosRoutes` en `/pagos` |

### Frontend (`/Users/joaquinmorales13a06/Desktop/Reinar/frontend`, rama `feat/pagos`)

| Archivo | Acción | Responsabilidad |
|---|---|---|
| `types/api.ts` | Modificar | Agregar `PagoListItem`, `FiltrosPagos` |
| `components/pagos/RegistrarPagoForm.tsx` | **Crear (mover)** | Form de pago (con `onSuccess?` adicional) — viene de `components/facturas/detalle/` |
| `components/facturas/detalle/PagosCard.tsx` | Modificar | Actualizar import del form |
| `components/facturas/detalle/RegistrarPagoForm.tsx` | **Eliminar** | Reemplazado por la versión movida |
| `hooks/use-pagos.ts` | Modificar | Agregar `useListadoPagos` + `useFacturasPendientes`; extender invalidaciones de `useCrearPago`/`useEliminarPago` |
| `components/pagos/PagosFilters.tsx` | **Crear** | Búsqueda + chips método + rango fechas + selector cliente |
| `components/pagos/PagosTabla.tsx` | **Crear** | Tabla con paginación, expansión a detalle, eliminar inline (ADMIN) |
| `components/pagos/PagoDetallePanel.tsx` | **Crear** | Card inline expandida con notas/referencia (datos del listado) |
| `components/pagos/SelectorFacturaPendiente.tsx` | **Crear** | Typeahead client-side |
| `components/pagos/FacturaSeleccionadaCard.tsx` | **Crear** | Card destacada de factura seleccionada en `/pagos/nuevo` |
| `app/(dashboard)/pagos/page.tsx` | **Crear** | Página listado |
| `app/(dashboard)/pagos/nuevo/page.tsx` | **Crear** | Página registro standalone |

---

# PARTE A — Backend

> Cambiar de directorio: `cd /Users/joaquinmorales13a06/Desktop/Reinar/server`

## Task A1: Crear rama backend

**Files:**
- N/A (operación de git)

- [ ] **Step 1: Verificar rama actual y estado limpio**

Run: `cd /Users/joaquinmorales13a06/Desktop/Reinar/server && git status -s && git rev-parse --abbrev-ref HEAD`
Expected: vacío (sin cambios pendientes) y la rama actual (probablemente `main`).

- [ ] **Step 2: Actualizar main y crear rama**

Run:
```bash
git checkout main
git pull --ff-only origin main
git checkout -b feat/pagos-listado-global
```
Expected: `Switched to a new branch 'feat/pagos-listado-global'`.

---

## Task A2: Agregar `listarPagosGlobalQuery` al schema

**Files:**
- Modify: `src/modules/pagos/pagos.schemas.ts`

- [ ] **Step 1: Reemplazar contenido del archivo**

Sobrescribir `src/modules/pagos/pagos.schemas.ts` con:

```typescript
import { z } from 'zod'
import { Prisma } from '@prisma/client'

const Decimal = Prisma.Decimal

// ── Crear pago ──────────────────────────────────────────────────────────────────

const decimalPositivo = z
  .string()
  .regex(/^\d+(\.\d{1,2})?$/, 'Debe ser un número decimal con máximo 2 decimales')
  .refine((v) => { try { return new Decimal(v).gt(0) } catch { return false } }, 'El monto debe ser mayor a cero')

export const crearPagoSchema = z.object({
  monto:      decimalPositivo,
  fecha:      z.string().datetime(),
  // ANTICIPO se omite intencionalmente: es creado por el service de cotizaciones al aprobar, nunca por el cliente HTTP
  metodoPago: z.enum(['EFECTIVO', 'TRANSFERENCIA', 'CHEQUE', 'TARJETA', 'OTRO']),
  referencia: z.string().optional(),
  notas:      z.string().optional(),
})

export type CrearPagoInput = z.infer<typeof crearPagoSchema>

// ── Listar pagos global ─────────────────────────────────────────────────────────
// Endpoint nuevo (rama 13 del frontend) que permite ver todos los pagos sin tener
// que entrar a una factura específica. Incluye ANTICIPO como valor filtrable para
// que ADMIN/GERENTE puedan auditar los depósitos creados internamente al aprobar
// cotizaciones — no se permite crear ANTICIPO vía HTTP pero sí filtrarlo.

export const listarPagosGlobalQuery = z.object({
  page:       z.coerce.number().int().min(1).default(1),
  limit:      z.coerce.number().int().min(1).max(100).default(20),
  busqueda:   z.string().trim().min(1).optional(),
  metodoPago: z.enum(['EFECTIVO', 'TRANSFERENCIA', 'CHEQUE', 'TARJETA', 'OTRO', 'ANTICIPO']).optional(),
  clienteId:  z.string().cuid().optional(),
  fechaDesde: z.string().datetime().optional(),
  fechaHasta: z.string().datetime().optional(),
})

export type ListarPagosGlobalQuery = z.infer<typeof listarPagosGlobalQuery>
```

- [ ] **Step 2: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

Run:
```bash
git add src/modules/pagos/pagos.schemas.ts
git commit -m "feat(pagos): agregar listarPagosGlobalQuery schema"
```

---

## Task A3: Agregar `listarPagosGlobal` al service

**Files:**
- Modify: `src/modules/pagos/pagos.service.ts`

- [ ] **Step 1: Actualizar imports**

En `src/modules/pagos/pagos.service.ts`, reemplazar la línea actual de imports de schemas:

```typescript
import { CrearPagoInput } from './pagos.schemas'
```

con:

```typescript
import { CrearPagoInput, ListarPagosGlobalQuery } from './pagos.schemas'
```

- [ ] **Step 2: Agregar la función al final del archivo**

Anexar al final de `src/modules/pagos/pagos.service.ts`:

```typescript
// Listado global de pagos (rama 13). Hace join hasta cliente para soportar
// búsqueda por número de factura o nombre del cliente, y para que el frontend
// pueda mostrar el contexto sin requests adicionales. orderBy fecha desc
// porque el caso de uso típico es "qué pagos entraron recientemente".
export async function listarPagosGlobal(filtros: ListarPagosGlobalQuery) {
  const { page, limit, busqueda, metodoPago, clienteId, fechaDesde, fechaHasta } = filtros

  const where: Prisma.PagoWhereInput = {
    ...(metodoPago && { metodoPago }),
    ...((fechaDesde || fechaHasta) && {
      fecha: {
        ...(fechaDesde && { gte: new Date(fechaDesde) }),
        ...(fechaHasta && { lte: new Date(fechaHasta) }),
      },
    }),
    ...((clienteId || busqueda) && {
      factura: {
        ...(clienteId && { clienteId }),
        ...(busqueda && {
          OR: [
            { numeroFactura: { contains: busqueda, mode: 'insensitive' } },
            { cliente: { razonSocial: { contains: busqueda, mode: 'insensitive' } } },
            { cliente: { nombre:      { contains: busqueda, mode: 'insensitive' } } },
            { cliente: { apellido:    { contains: busqueda, mode: 'insensitive' } } },
          ],
        }),
      },
    }),
  }

  const skip = (page - 1) * limit

  const [data, total] = await Promise.all([
    prisma.pago.findMany({
      where,
      skip,
      take: limit,
      orderBy: { fecha: 'desc' },
      select: {
        id:         true,
        monto:      true,
        fecha:      true,
        metodoPago: true,
        referencia: true,
        notas:      true,
        createdAt:  true,
        factura: {
          select: {
            id:            true,
            numeroFactura: true,
            estado:        true,
            // 5 campos del cliente porque EMPRESA usa razonSocial y PARTICULAR
            // usa nombre + apellido. Mismo patrón que FacturaListItem.
            cliente: {
              select: {
                id:          true,
                tipo:        true,
                razonSocial: true,
                nombre:      true,
                apellido:    true,
              },
            },
          },
        },
      },
    }),
    prisma.pago.count({ where }),
  ])

  return { data, meta: { page, limit, total } }
}
```

- [ ] **Step 3: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores.

- [ ] **Step 4: Commit**

Run:
```bash
git add src/modules/pagos/pagos.service.ts
git commit -m "feat(pagos): agregar listarPagosGlobal service"
```

---

## Task A4: Crear el controller

**Files:**
- Create: `src/modules/pagos/pagos.controller.ts`

- [ ] **Step 1: Crear archivo**

Escribir `src/modules/pagos/pagos.controller.ts` con:

```typescript
import { Request, Response, NextFunction } from 'express'
import * as service from './pagos.service'
import { ListarPagosGlobalQuery } from './pagos.schemas'

// El módulo crea/elimina pagos por factura desde facturas.controller.ts.
// Este controller solo expone el listado global, que es independiente de
// una factura específica.
export async function listarPagosGlobal(req: Request, res: Response, next: NextFunction): Promise<void> {
  try {
    const result = await service.listarPagosGlobal(req.query as unknown as ListarPagosGlobalQuery)
    res.json({ success: true, data: result.data, meta: result.meta })
  } catch (err) { next(err) }
}
```

- [ ] **Step 2: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

Run:
```bash
git add src/modules/pagos/pagos.controller.ts
git commit -m "feat(pagos): agregar controller del listado global"
```

---

## Task A5: Crear las routes del módulo

**Files:**
- Create: `src/modules/pagos/pagos.routes.ts`

- [ ] **Step 1: Crear archivo**

Escribir `src/modules/pagos/pagos.routes.ts` con:

```typescript
import { Router } from 'express'
import { authenticate, requireRol, validateQuery } from '../../middleware/auth.middleware'
import { listarPagosGlobalQuery } from './pagos.schemas'
import * as ctrl from './pagos.controller'

const router = Router()

const todos = ['ADMIN', 'GERENTE', 'OPERADOR', 'LOGISTICA', 'VISUALIZADOR'] as const

// Solo listado. Crear y eliminar pagos siguen viviendo en facturas.routes.ts
// como sub-recurso de factura — el listado global no requiere :facturaId,
// por eso necesita su propio router montado en /pagos.
router.get('/', authenticate, requireRol(...todos), validateQuery(listarPagosGlobalQuery), ctrl.listarPagosGlobal)

export default router
```

- [ ] **Step 2: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

Run:
```bash
git add src/modules/pagos/pagos.routes.ts
git commit -m "feat(pagos): agregar routes del listado global"
```

---

## Task A6: Montar el router en index.ts

**Files:**
- Modify: `src/index.ts`

- [ ] **Step 1: Agregar import**

Después de la línea `import facturasRoutes from './modules/facturas/facturas.routes'`, agregar:

```typescript
import pagosRoutes from './modules/pagos/pagos.routes'
```

- [ ] **Step 2: Montar el router**

Después de la línea `api.use('/facturas', facturasRoutes)`, agregar:

```typescript
api.use('/pagos', pagosRoutes)
```

- [ ] **Step 3: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores.

- [ ] **Step 4: Smoke test manual**

Arrancar el servidor en otra terminal:
```bash
pnpm dev
```

Esperar a que arranque. Luego, en otra terminal, probar el endpoint con un token válido. Si no se tiene token a mano:

```bash
# Login (ajustar email/password reales)
TOKEN=$(curl -s -X POST http://localhost:3000/api/v1/auth/iniciar-sesion \
  -H "Content-Type: application/json" \
  -d '{"email":"<email>","password":"<password>"}' | jq -r '.data.accessToken')

# Llamada al endpoint nuevo
curl -s "http://localhost:3000/api/v1/pagos?page=1&limit=5" \
  -H "Authorization: Bearer $TOKEN" | jq
```

Expected: respuesta `{ "success": true, "data": [...], "meta": { "page": 1, "limit": 5, "total": <n> } }`. Si la BD tiene pagos, deberían venir con `factura.cliente.razonSocial` (o nombre+apellido).

Probar también un filtro:
```bash
curl -s "http://localhost:3000/api/v1/pagos?metodoPago=TRANSFERENCIA" \
  -H "Authorization: Bearer $TOKEN" | jq '.data | length, .meta.total'
```

Expected: solo pagos con `metodoPago: "TRANSFERENCIA"`.

Detener el server (Ctrl+C en su terminal).

- [ ] **Step 5: Commit**

Run:
```bash
git add src/index.ts
git commit -m "feat(pagos): montar /api/v1/pagos en el router principal"
```

---

## Task A7: PR del backend

- [ ] **Step 1: Push y crear PR**

Run:
```bash
git push -u origin feat/pagos-listado-global
gh pr create --title "feat(pagos): endpoint global GET /api/v1/pagos" --body "$(cat <<'EOF'
## Summary
- Nuevo endpoint `GET /api/v1/pagos` con filtros (page, limit, busqueda, metodoPago, clienteId, fechaDesde, fechaHasta).
- Sin migración Prisma; reutiliza el modelo `Pago` existente.
- Incluye `ANTICIPO` como valor filtrable (no creable vía HTTP).
- Devuelve los 5 campos del cliente para que el frontend componga el nombre según `tipo`.

Habilita la Rama 13 del frontend (`feat/pagos`). **Mergear este PR antes** que el del frontend.

## Test plan
- [ ] `pnpm tsc --noEmit` pasa.
- [ ] `pnpm dev` arranca sin warnings.
- [ ] `curl GET /api/v1/pagos` con token válido devuelve `{ success: true, data: [], meta: {...} }`.
- [ ] Filtrar por `metodoPago=TRANSFERENCIA` reduce el resultado correctamente.
- [ ] Filtrar por `busqueda=FAC-2026` matchea por número de factura.
- [ ] Filtrar por `clienteId=<cuid>` solo muestra pagos de facturas de ese cliente.
- [ ] Filtrar por `fechaDesde` y `fechaHasta` recorta el rango.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

Expected: PR URL.

- [ ] **Step 2: Esperar merge antes de seguir con frontend**

El PR del backend debe ser revisado y mergeado antes de proseguir con la Parte B. El deploy del backend también debe estar arriba en el ambiente donde se hará el smoke test del frontend.

Tras el merge:
```bash
git checkout main
git pull --ff-only origin main
```

---

# PARTE B — Frontend

> Cambiar de directorio: `cd /Users/joaquinmorales13a06/Desktop/Reinar/frontend`
> Asegurarse de estar en la rama `feat/pagos` (ya creada).

## Task B1: Verificar rama frontend

**Files:**
- N/A

- [ ] **Step 1: Confirmar contexto**

Run:
```bash
cd /Users/joaquinmorales13a06/Desktop/Reinar/frontend
git status -s
git rev-parse --abbrev-ref HEAD
```
Expected: working tree limpio y rama actual `feat/pagos`.

- [ ] **Step 2: Confirmar que el spec está commiteado**

Run: `git log --oneline -3`
Expected: ver el commit `docs(pagos): diseño de la rama 13 …` en el historial reciente.

---

## Task B2: Extender `types/api.ts` con `PagoListItem` y `FiltrosPagos`

**Files:**
- Modify: `types/api.ts`

- [ ] **Step 1: Agregar tipos**

Localizar el bloque que termina con `CrearPagoDto` (alrededor de la línea 1010). Inmediatamente después de la definición de `CrearPagoDto` (antes del comentario `// ── Actas y Recepciones (RAMA 12) ─`), insertar:

```typescript
// ── Listado global de pagos (RAMA 13) ──────────────────────────────────
// Shape devuelta por GET /api/v1/pagos. El backend embebe factura+cliente
// (5 campos del cliente) para evitar requests adicionales y soportar
// la búsqueda libre por número de factura o nombre de cliente.
export type PagoListItem = {
  id: string;
  monto: string;
  fecha: string;
  metodoPago: MetodoPago;
  referencia: string | null;
  notas: string | null;
  createdAt: string;
  factura: {
    id: string;
    numeroFactura: string;
    estado: EstadoFactura;
    cliente: {
      id: string;
      tipo: 'EMPRESA' | 'PARTICULAR';
      razonSocial: string | null;
      nombre: string | null;
      apellido: string | null;
    };
  };
};

export type FiltrosPagos = {
  page?: number;
  limit?: number;
  busqueda?: string;
  metodoPago?: MetodoPago;
  clienteId?: string;
  fechaDesde?: string;
  fechaHasta?: string;
};
```

- [ ] **Step 2: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

Run:
```bash
git add types/api.ts
git commit -m "feat(pagos): tipos PagoListItem y FiltrosPagos"
```

---

## Task B3: Mover `RegistrarPagoForm` a `components/pagos/` con `onSuccess`

**Files:**
- Create: `components/pagos/RegistrarPagoForm.tsx`
- Delete: `components/facturas/detalle/RegistrarPagoForm.tsx`
- Modify: `components/facturas/detalle/PagosCard.tsx` (actualizar import)

- [ ] **Step 1: Crear directorio si no existe**

Run: `mkdir -p components/pagos`

- [ ] **Step 2: Crear el archivo movido con la prop nueva**

Escribir `components/pagos/RegistrarPagoForm.tsx` con:

```typescript
'use client';

import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import Decimal from 'decimal.js';
import { Icon } from '@/components/ui/Icon';
import { ConfirmRow } from '@/components/ui/ConfirmRow';
import { registrarPagoSchema, type RegistrarPagoForm as Form } from '@/lib/schemas/factura';
import { useCrearPago } from '@/hooks/use-pagos';
import { formatCurrency } from '@/lib/utils';

const METODOS: { value: Form['metodoPago']; label: string }[] = [
  { value: 'EFECTIVO',       label: 'Efectivo' },
  { value: 'TRANSFERENCIA',  label: 'Transferencia' },
  { value: 'CHEQUE',         label: 'Cheque' },
  { value: 'TARJETA',        label: 'Tarjeta' },
  { value: 'OTRO',           label: 'Otro' },
];

// Fecha de hoy en TZ El Salvador (UTC-6). Convertimos a YYYY-MM-DD para el input date.
function hoyEnSv(): string {
  const now = new Date();
  const sv = new Date(now.toLocaleString('en-US', { timeZone: 'America/El_Salvador' }));
  const y = sv.getFullYear();
  const m = String(sv.getMonth() + 1).padStart(2, '0');
  const d = String(sv.getDate()).padStart(2, '0');
  return `${y}-${m}-${d}`;
}

// onClose: cerrar form inline (usado por PagosCard del detalle de factura).
// onSuccess: navegar/refrescar después del éxito (usado por /pagos/nuevo).
// Si onSuccess está definido lo llamamos; si no, llamamos onClose. Permite
// reutilizar el mismo componente en ambos modos sin duplicar lógica de
// validación, formato de fecha SV y manejo de sobrepago.
type Props = {
  facturaId: string;
  saldoPendiente: string;
  onClose?: () => void;
  onSuccess?: () => void;
};

export function RegistrarPagoForm({ facturaId, saldoPendiente, onClose, onSuccess }: Props) {
  const crear = useCrearPago();
  const { register, handleSubmit, watch, formState: { errors }, setError } = useForm<Form>({
    resolver: zodResolver(registrarPagoSchema),
    defaultValues: {
      monto: new Decimal(saldoPendiente).toFixed(2),
      fecha: hoyEnSv(),
      metodoPago: 'TRANSFERENCIA',
    },
  });

  const monto = watch('monto');
  const saldo = new Decimal(saldoPendiente);
  // Sobrepago: el backend permite registrarlo; advertimos al usuario antes
  // de confirmar para evitar accidentes con montos mal tipeados.
  const sobrepago = (() => {
    try { return new Decimal(monto).gt(saldo); } catch { return false; }
  })();

  async function onSubmit(values: Form) {
    try {
      // Convertimos fecha YYYY-MM-DD a ISO datetime: el schema del backend
      // exige z.string().datetime().
      const fechaIso = new Date(values.fecha + 'T12:00:00.000Z').toISOString();
      await crear.mutateAsync({
        facturaId,
        data: { ...values, fecha: fechaIso },
      });
      if (onSuccess) onSuccess();
      else if (onClose) onClose();
    } catch (err) {
      const anyErr = err as { response?: { data?: { error?: { message?: string } } } };
      const msg = anyErr?.response?.data?.error?.message;
      if (msg) setError('monto', { message: msg });
    }
  }

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="bg-bg-sunken border border-bd rounded-md p-4 space-y-3 mb-3">
      <div className="flex items-center justify-between">
        <h4 className="text-sm font-medium text-tx">Registrar pago</h4>
        {onClose && (
          <button type="button" onClick={onClose} className="text-tx-3 hover:text-tx" title="Cerrar">
            <Icon name="x" size={14} />
          </button>
        )}
      </div>
      <div className="grid grid-cols-1 sm:grid-cols-3 gap-3 text-sm">
        <div>
          <label className="block text-xs text-tx-3 mb-1">Monto</label>
          <input
            type="text"
            inputMode="decimal"
            {...register('monto')}
            className="w-full px-2 py-1.5 rounded border border-bd bg-bg font-mono"
          />
          {errors.monto && <p className="text-xs text-danger mt-1">{errors.monto.message}</p>}
        </div>
        <div>
          <label className="block text-xs text-tx-3 mb-1">Fecha</label>
          <input
            type="date"
            {...register('fecha')}
            className="w-full px-2 py-1.5 rounded border border-bd bg-bg font-mono"
          />
          {errors.fecha && <p className="text-xs text-danger mt-1">{errors.fecha.message}</p>}
        </div>
        <div>
          <label className="block text-xs text-tx-3 mb-1">Método</label>
          <select {...register('metodoPago')} className="w-full px-2 py-1.5 rounded border border-bd bg-bg">
            {METODOS.map((m) => <option key={m.value} value={m.value}>{m.label}</option>)}
          </select>
        </div>
        <div className="sm:col-span-3">
          <label className="block text-xs text-tx-3 mb-1">Referencia</label>
          <input
            type="text"
            placeholder="N° transferencia, cheque…"
            {...register('referencia')}
            className="w-full px-2 py-1.5 rounded border border-bd bg-bg"
          />
        </div>
        <div className="sm:col-span-3">
          <label className="block text-xs text-tx-3 mb-1">Notas</label>
          <textarea
            rows={2}
            {...register('notas')}
            className="w-full px-2 py-1.5 rounded border border-bd bg-bg text-sm"
          />
          {errors.notas && <p className="text-xs text-danger mt-1">{errors.notas.message}</p>}
        </div>
      </div>
      {sobrepago && (
        <div className="flex items-center gap-2 bg-warn-soft text-warn text-xs rounded-md px-3 py-2">
          <Icon name="alertTriangle" size={12} />
          <span>El monto supera el saldo pendiente ({formatCurrency(saldoPendiente)}). Se registrará igual y la factura quedará en sobrepago.</span>
        </div>
      )}
      <ConfirmRow
        message="¿Registrar este pago? El saldo de la factura se recalculará."
        confirmLabel={crear.isPending ? 'Guardando…' : 'Registrar pago'}
        variant="primary"
        onCancel={onClose ?? onSuccess ?? (() => {})}
        onConfirm={handleSubmit(onSubmit)}
      />
    </form>
  );
}
```

- [ ] **Step 3: Eliminar el archivo viejo**

Run: `rm components/facturas/detalle/RegistrarPagoForm.tsx`

- [ ] **Step 4: Actualizar import en `PagosCard.tsx`**

En `components/facturas/detalle/PagosCard.tsx`, reemplazar la línea:

```typescript
import { RegistrarPagoForm } from './RegistrarPagoForm';
```

por:

```typescript
import { RegistrarPagoForm } from '@/components/pagos/RegistrarPagoForm';
```

- [ ] **Step 5: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores.

- [ ] **Step 6: Commit**

Run:
```bash
git add components/pagos/RegistrarPagoForm.tsx components/facturas/detalle/RegistrarPagoForm.tsx components/facturas/detalle/PagosCard.tsx
git commit -m "refactor(pagos): mover RegistrarPagoForm a components/pagos con onSuccess"
```

---

## Task B4: Extender `hooks/use-pagos.ts` con `useListadoPagos` y `useFacturasPendientes`

**Files:**
- Modify: `hooks/use-pagos.ts`

- [ ] **Step 1: Reemplazar contenido del archivo**

Sobrescribir `hooks/use-pagos.ts` con:

```typescript
'use client';

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { toast } from 'sonner';
import Decimal from 'decimal.js';
import api from '@/lib/api';
import type {
  ApiResponse,
  PaginatedResponse,
  Pago,
  CrearPagoDto,
  PagoListItem,
  FiltrosPagos,
  FacturaListItem,
} from '@/types/api';

function extractErrorMessage(err: unknown, fallback: string): string {
  const anyErr = err as { response?: { data?: { error?: { message?: string } } } };
  return anyErr?.response?.data?.error?.message ?? fallback;
}

// ─── Pagos por factura (existente) ─────────────────────────────────────

export function useListarPagos(facturaId: string | null | undefined) {
  return useQuery({
    queryKey: ['pagos', facturaId],
    queryFn: () =>
      api
        .get<ApiResponse<Pago[]>>(`/facturas/${facturaId}/pagos`)
        .then((r) => {
          if (!r.data.success) throw new Error(r.data.error.message);
          return r.data.data;
        }),
    enabled: !!facturaId,
  });
}

// ─── Listado global (RAMA 13) ──────────────────────────────────────────
// QueryKey separado de ['pagos', facturaId] porque son consultas distintas
// (lista global con filtros vs. pagos de una factura). placeholderData
// mantiene la página anterior visible mientras paginamos para evitar
// el flash de loading.
export function useListadoPagos(filtros: FiltrosPagos) {
  return useQuery({
    queryKey: ['pagos-listado', filtros],
    queryFn: () =>
      api
        .get<PaginatedResponse<PagoListItem>>('/pagos', { params: filtros })
        .then((r) => r.data),
    placeholderData: (prev) => prev,
  });
}

// ─── Facturas elegibles para registrar un pago (typeahead) ─────────────
// El backend no soporta filtrar por múltiples estados ni buscar por
// numeroFactura en GET /facturas, así que cargamos hasta 100 y filtramos
// client-side. El volumen esperado (decenas de facturas pendientes) hace
// que esto sea suficiente; si crece, se migra a un endpoint dedicado.
export function useFacturasPendientes() {
  return useQuery({
    queryKey: ['facturas-pendientes'],
    queryFn: () =>
      api
        .get<PaginatedResponse<FacturaListItem>>('/facturas', { params: { limit: 100 } })
        .then((r) =>
          r.data.data.filter(
            (f) =>
              ['PENDIENTE', 'PARCIAL', 'VENCIDA'].includes(f.estado) &&
              new Decimal(f.saldoPendiente).gt(0),
          ),
        ),
    staleTime: 30_000,
  });
}

// ─── Mutations ─────────────────────────────────────────────────────────

export function useCrearPago() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: ({ facturaId, data }: { facturaId: string; data: CrearPagoDto }) =>
      api
        .post<ApiResponse<Pago>>(`/facturas/${facturaId}/pagos`, data)
        .then((r) => {
          if (!r.data.success) throw new Error(r.data.error.message);
          return r.data.data;
        }),
    onSuccess: (_pago, { facturaId }) => {
      // Invalidamos los pagos de esa factura (detalle), el listado global
      // (rama 13) y la factura misma (el backend recalcula montoPagado,
      // saldoPendiente y estado tras crear un pago). El listado de
      // facturas también porque su columna "Saldo" cambia.
      qc.invalidateQueries({ queryKey: ['pagos', facturaId] });
      qc.invalidateQueries({ queryKey: ['pagos-listado'] });
      qc.invalidateQueries({ queryKey: ['factura', facturaId] });
      qc.invalidateQueries({ queryKey: ['facturas'] });
      qc.invalidateQueries({ queryKey: ['facturas-pendientes'] });
      toast.success('Pago registrado.');
    },
    onError: (err) => {
      toast.error(extractErrorMessage(err, 'No se pudo registrar el pago.'));
    },
  });
}

export function useEliminarPago() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: ({ facturaId, pagoId }: { facturaId: string; pagoId: string }) =>
      api
        .delete<ApiResponse<unknown>>(`/facturas/${facturaId}/pagos/${pagoId}`)
        .then((r) => {
          if (!r.data.success) throw new Error(r.data.error.message);
        }),
    onSuccess: (_d, { facturaId }) => {
      qc.invalidateQueries({ queryKey: ['pagos', facturaId] });
      qc.invalidateQueries({ queryKey: ['pagos-listado'] });
      qc.invalidateQueries({ queryKey: ['factura', facturaId] });
      qc.invalidateQueries({ queryKey: ['facturas'] });
      qc.invalidateQueries({ queryKey: ['facturas-pendientes'] });
      toast.success('Pago eliminado.');
    },
    onError: (err) => {
      toast.error(extractErrorMessage(err, 'No se pudo eliminar el pago.'));
    },
  });
}
```

- [ ] **Step 2: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

Run:
```bash
git add hooks/use-pagos.ts
git commit -m "feat(pagos): hooks useListadoPagos y useFacturasPendientes"
```

---

## Task B5: Crear `components/pagos/PagosFilters.tsx`

**Files:**
- Create: `components/pagos/PagosFilters.tsx`

- [ ] **Step 1: Crear archivo**

Escribir `components/pagos/PagosFilters.tsx` con:

```typescript
'use client';

import { useState } from 'react';
import { FilterBar } from '@/components/ui/FilterBar';
import { Icon } from '@/components/ui/Icon';
import { useClientes } from '@/hooks/use-clientes';
import type { FiltrosPagos, MetodoPago } from '@/types/api';

// ANTICIPO aparece como chip filtrable (no creable). Permite auditar los
// depósitos generados internamente al aprobar cotizaciones.
const METODOS: MetodoPago[] = ['EFECTIVO', 'TRANSFERENCIA', 'CHEQUE', 'TARJETA', 'OTRO', 'ANTICIPO'];

function metodoLabel(m: MetodoPago): string {
  return m[0] + m.slice(1).toLowerCase();
}

type Props = {
  value: FiltrosPagos;
  onChange: (next: FiltrosPagos) => void;
};

export function PagosFilters({ value, onChange }: Props) {
  // Cliente picker se carga lazy al abrir el dropdown — evita una request
  // a /clientes en cada render del listado.
  const [clienteOpen, setClienteOpen] = useState(false);
  const { data: clientesData } = useClientes(clienteOpen ? { limit: 100 } : undefined);
  const clientes = clientesData?.data ?? [];

  const chips = METODOS.map((m) => ({
    label: metodoLabel(m),
    active: value.metodoPago === m,
    onToggle: () => onChange({ ...value, metodoPago: value.metodoPago === m ? undefined : m, page: 1 }),
  }));

  function setBusqueda(v: string) {
    onChange({ ...value, busqueda: v || undefined, page: 1 });
  }

  function setFecha(key: 'fechaDesde' | 'fechaHasta', v: string) {
    // El backend espera ISO datetime. Convertimos a las 00:00 UTC del día.
    const iso = v ? new Date(v + 'T00:00:00.000Z').toISOString() : undefined;
    onChange({ ...value, [key]: iso, page: 1 });
  }

  function setCliente(id: string) {
    onChange({ ...value, clienteId: id || undefined, page: 1 });
  }

  function clear() {
    onChange({ page: 1, limit: value.limit });
  }

  // Convertir ISO de vuelta a YYYY-MM-DD para el input. Si está vacío, ''.
  const desdeInput = value.fechaDesde ? value.fechaDesde.slice(0, 10) : '';
  const hastaInput = value.fechaHasta ? value.fechaHasta.slice(0, 10) : '';

  function nombreCliente(c: typeof clientes[number]): string {
    if (c.tipo === 'EMPRESA') return c.razonSocial ?? '—';
    return [c.nombre, c.apellido].filter(Boolean).join(' ') || '—';
  }

  return (
    <div className="space-y-2">
      <FilterBar
        search={value.busqueda ?? ''}
        onSearch={setBusqueda}
        placeholder="Buscar por número de factura o cliente…"
        chips={chips}
        onClear={clear}
      />
      <div className="flex flex-wrap items-center gap-2 px-4 py-2 border border-bd border-t-0 bg-bg-sunken text-sm">
        <Icon name="filter" size={12} />
        <label className="text-tx-3 text-xs">Desde:</label>
        <input
          type="date"
          value={desdeInput}
          onChange={(e) => setFecha('fechaDesde', e.target.value)}
          className="text-xs px-2 py-1 rounded border border-bd bg-bg text-tx font-mono"
        />
        <label className="text-tx-3 text-xs">Hasta:</label>
        <input
          type="date"
          value={hastaInput}
          onChange={(e) => setFecha('fechaHasta', e.target.value)}
          className="text-xs px-2 py-1 rounded border border-bd bg-bg text-tx font-mono"
        />
        <label className="text-tx-3 text-xs ml-2">Cliente:</label>
        <select
          value={value.clienteId ?? ''}
          onFocus={() => setClienteOpen(true)}
          onChange={(e) => setCliente(e.target.value)}
          className="text-xs px-2 py-1 rounded border border-bd bg-bg text-tx min-w-40"
        >
          <option value="">Todos</option>
          {clientes.map((c) => (
            <option key={c.id} value={c.id}>{nombreCliente(c)}</option>
          ))}
        </select>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores. **Si falla**: verificar la firma real de `useClientes` y ajustar — el hook puede aceptar parámetros diferentes; en ese caso adaptar la línea `useClientes(...)` para que devuelva un listado con `.data`. Si el shape no calza, leer `hooks/use-clientes.ts` y traducir.

- [ ] **Step 3: Commit**

Run:
```bash
git add components/pagos/PagosFilters.tsx
git commit -m "feat(pagos): PagosFilters con búsqueda, chips, rango y cliente"
```

---

## Task B6: Crear `components/pagos/PagoDetallePanel.tsx`

**Files:**
- Create: `components/pagos/PagoDetallePanel.tsx`

- [ ] **Step 1: Crear archivo**

Escribir `components/pagos/PagoDetallePanel.tsx` con:

```typescript
'use client';

import { formatDate } from '@/lib/utils';
import type { PagoListItem } from '@/types/api';

type Props = { pago: PagoListItem };

// Panel embebido como fila acordeón en PagosTabla. Solo muestra datos que
// ya vienen del listado — no hace requests adicionales. Las notas largas
// no caben en la columna de la tabla; aquí se muestran completas.
export function PagoDetallePanel({ pago }: Props) {
  return (
    <div className="bg-bg-sunken px-4 py-3 border-t border-bd text-sm space-y-2">
      <div className="grid grid-cols-1 sm:grid-cols-2 gap-x-6 gap-y-2">
        <div>
          <div className="text-2xs uppercase tracking-wider text-tx-3 mb-0.5">ID del pago</div>
          <div className="font-mono text-xs text-tx-2 break-all">{pago.id}</div>
        </div>
        <div>
          <div className="text-2xs uppercase tracking-wider text-tx-3 mb-0.5">Registrado</div>
          <div className="font-mono text-xs text-tx-2">{formatDate(pago.createdAt)}</div>
        </div>
        <div className="sm:col-span-2">
          <div className="text-2xs uppercase tracking-wider text-tx-3 mb-0.5">Referencia</div>
          <div className="font-mono text-xs text-tx-2 break-all">{pago.referencia ?? '—'}</div>
        </div>
        <div className="sm:col-span-2">
          <div className="text-2xs uppercase tracking-wider text-tx-3 mb-0.5">Notas</div>
          <div className="text-xs text-tx-2 whitespace-pre-wrap">{pago.notas ?? '—'}</div>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

Run:
```bash
git add components/pagos/PagoDetallePanel.tsx
git commit -m "feat(pagos): PagoDetallePanel acordeón con notas y referencia"
```

---

## Task B7: Crear `components/pagos/PagosTabla.tsx`

**Files:**
- Create: `components/pagos/PagosTabla.tsx`

- [ ] **Step 1: Crear archivo**

Escribir `components/pagos/PagosTabla.tsx` con:

```typescript
'use client';

import { Fragment, useState } from 'react';
import Link from 'next/link';
import { Icon } from '@/components/ui/Icon';
import { Badge } from '@/components/ui/Badge';
import { EmptyState } from '@/components/ui/EmptyState';
import { Spinner } from '@/components/ui/Spinner';
import { Pagination } from '@/components/ui/Pagination';
import { ConfirmRow } from '@/components/ui/ConfirmRow';
import { FacturaEstadoBadge } from '@/components/facturas/FacturaEstadoBadge';
import { PagoDetallePanel } from './PagoDetallePanel';
import { useEliminarPago } from '@/hooks/use-pagos';
import { formatCurrency, formatDate } from '@/lib/utils';
import type { PagoListItem } from '@/types/api';

// Mismo helper que FacturasTabla — duplicación intencional para no extraer
// 6 líneas a un util compartido. EMPRESA usa razonSocial; PARTICULAR usa
// nombre + apellido. "—" si falta el dato base.
function nombreCliente(c: PagoListItem['factura']['cliente']): string {
  if (c.tipo === 'EMPRESA') return c.razonSocial ?? '—';
  return [c.nombre, c.apellido].filter(Boolean).join(' ') || '—';
}

type Props = {
  pagos: PagoListItem[];
  loading: boolean;
  page: number;
  pageSize: number;
  total: number;
  onPage: (p: number) => void;
  canDelete: boolean;
  hasFilters: boolean;
};

export function PagosTabla({ pagos, loading, page, pageSize, total, onPage, canDelete, hasFilters }: Props) {
  const [expanded, setExpanded] = useState<string | null>(null);
  const [confirmDelete, setConfirmDelete] = useState<string | null>(null);
  const eliminar = useEliminarPago();

  if (loading) {
    return <div className="flex justify-center py-12"><Spinner /></div>;
  }
  if (pagos.length === 0) {
    return hasFilters ? (
      <EmptyState icon="filter" title="Sin resultados" message="No hay pagos con esos filtros." />
    ) : (
      <EmptyState icon="dollar" title="Sin pagos" message="Aún no se han registrado pagos." />
    );
  }

  return (
    <div className="border border-bd rounded-b-md overflow-x-auto">
      <table className="w-full text-sm">
        <thead className="bg-bg-sunken text-2xs uppercase tracking-wider text-tx-3">
          <tr>
            <th className="text-left font-medium px-4 py-2.5 w-36">ID</th>
            <th className="text-left font-medium px-4 py-2.5 w-40">Factura</th>
            <th className="text-left font-medium px-4 py-2.5">Cliente</th>
            <th className="text-left font-medium px-4 py-2.5 w-36">Método</th>
            <th className="text-left font-medium px-4 py-2.5">Referencia</th>
            <th className="text-left font-medium px-4 py-2.5 w-28">Fecha</th>
            <th className="text-right font-medium px-4 py-2.5 w-32">Monto</th>
            <th className="px-4 py-2.5 w-16"></th>
          </tr>
        </thead>
        <tbody>
          {pagos.map((p) => {
            const isExpanded = expanded === p.id;
            // El backend rechaza eliminar pagos de facturas ANULADAS con 422.
            // Lo prevenimos en UI deshabilitando el botón.
            const facturaAnulada = p.factura.estado === 'ANULADA';
            return (
              <Fragment key={p.id}>
                <tr
                  className={`border-t border-bd hover:bg-bg-sunken cursor-pointer transition-colors ${isExpanded ? 'bg-bg-sunken' : ''}`}
                  onClick={() => setExpanded(isExpanded ? null : p.id)}
                >
                  <td className="px-4 py-2.5 font-mono text-xs text-tx-2">{p.id.slice(0, 12)}…</td>
                  <td className="px-4 py-2.5">
                    <Link
                      href={`/facturas/${p.factura.id}`}
                      onClick={(e) => e.stopPropagation()}
                      className="font-mono text-xs text-tx hover:underline"
                    >
                      {p.factura.numeroFactura}
                    </Link>
                    <div className="mt-0.5"><FacturaEstadoBadge estado={p.factura.estado} /></div>
                  </td>
                  <td className="px-4 py-2.5 text-tx">{nombreCliente(p.factura.cliente)}</td>
                  <td className="px-4 py-2.5"><Badge status={p.metodoPago} /></td>
                  <td className="px-4 py-2.5 font-mono text-xs text-tx-3">{p.referencia ?? '—'}</td>
                  <td className="px-4 py-2.5 font-mono text-xs text-tx-2">{formatDate(p.fecha)}</td>
                  <td className="px-4 py-2.5 text-right tabular-nums font-medium text-ok">{formatCurrency(p.monto)}</td>
                  <td className="px-4 py-2.5 text-right">
                    {canDelete && (
                      <button
                        type="button"
                        disabled={facturaAnulada}
                        title={facturaAnulada ? 'Factura anulada — pago inmutable' : 'Eliminar pago'}
                        className="text-danger hover:bg-danger-soft rounded p-1 disabled:opacity-40 disabled:cursor-not-allowed disabled:hover:bg-transparent"
                        onClick={(e) => {
                          e.stopPropagation();
                          setConfirmDelete(p.id);
                        }}
                      >
                        <Icon name="trash" size={13} />
                      </button>
                    )}
                  </td>
                </tr>
                {isExpanded && (
                  <tr>
                    <td colSpan={8} className="p-0">
                      <PagoDetallePanel pago={p} />
                    </td>
                  </tr>
                )}
                {confirmDelete === p.id && (
                  <tr>
                    <td colSpan={8} className="px-4 py-3 border-t border-bd">
                      <ConfirmRow
                        message={`¿Eliminar este pago? El saldo de ${p.factura.numeroFactura} se recalculará.`}
                        confirmLabel={eliminar.isPending ? 'Eliminando…' : 'Eliminar pago'}
                        onCancel={() => setConfirmDelete(null)}
                        onConfirm={async () => {
                          try {
                            await eliminar.mutateAsync({ facturaId: p.factura.id, pagoId: p.id });
                            setConfirmDelete(null);
                          } catch {
                            // Toast manejado por el hook.
                          }
                        }}
                      />
                    </td>
                  </tr>
                )}
              </Fragment>
            );
          })}
        </tbody>
      </table>

      {total > pageSize && <Pagination page={page} pageSize={pageSize} total={total} onPage={onPage} />}
    </div>
  );
}
```

- [ ] **Step 2: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

Run:
```bash
git add components/pagos/PagosTabla.tsx
git commit -m "feat(pagos): PagosTabla con paginación, expansión y eliminar inline"
```

---

## Task B8: Crear `app/(dashboard)/pagos/page.tsx`

**Files:**
- Create: `app/(dashboard)/pagos/page.tsx`

- [ ] **Step 1: Crear directorio**

Run: `mkdir -p "app/(dashboard)/pagos/nuevo"`

- [ ] **Step 2: Crear el archivo**

Escribir `app/(dashboard)/pagos/page.tsx` con:

```typescript
'use client';

import { useState } from 'react';
import Link from 'next/link';
import Decimal from 'decimal.js';
import { PageHeader } from '@/components/ui/PageHeader';
import { Icon } from '@/components/ui/Icon';
import { PagosFilters } from '@/components/pagos/PagosFilters';
import { PagosTabla } from '@/components/pagos/PagosTabla';
import { useListadoPagos } from '@/hooks/use-pagos';
import { useAuthStore } from '@/stores/auth.store';
import { formatCurrency } from '@/lib/utils';
import type { FiltrosPagos } from '@/types/api';

export default function PagosPage() {
  const [filtros, setFiltros] = useState<FiltrosPagos>({ page: 1, limit: 20 });
  const user = useAuthStore((s) => s.user);
  // ADMIN es el único que puede eliminar pagos según el backend (requireRol('ADMIN')).
  // VISUALIZADOR no ve el botón de registrar — la ruta está protegida pero
  // ocultamos la UI para evitar acciones inútiles.
  const isAdmin = user?.rol === 'ADMIN';
  const puedeCrear = user?.rol !== 'VISUALIZADOR';

  const { data, isLoading } = useListadoPagos(filtros);
  const pagos = data?.data ?? [];
  const total = data?.meta.total ?? 0;
  // Total visible en el subtítulo: suma de la página actual. El total
  // cross-páginas requeriría otra query agregada que se reserva para la
  // rama de reportes (Rama 16).
  const sumaPagina = pagos.reduce((s, p) => s.add(p.monto), new Decimal(0));

  const hasFilters =
    !!filtros.busqueda ||
    !!filtros.metodoPago ||
    !!filtros.clienteId ||
    !!filtros.fechaDesde ||
    !!filtros.fechaHasta;

  return (
    <div>
      <PageHeader
        title="Pagos"
        subtitle={`${total} ${total === 1 ? 'pago' : 'pagos'} · ${formatCurrency(sumaPagina.toString())} en esta página`}
        actions={
          puedeCrear ? (
            <Link
              href="/pagos/nuevo"
              className="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-md text-sm font-medium bg-accent text-navy hover:bg-accent-dim"
            >
              <Icon name="plus" size={13} /> Registrar pago
            </Link>
          ) : null
        }
      />
      <PagosFilters value={filtros} onChange={setFiltros} />
      <PagosTabla
        pagos={pagos}
        loading={isLoading}
        page={filtros.page ?? 1}
        pageSize={filtros.limit ?? 20}
        total={total}
        onPage={(p) => setFiltros((f) => ({ ...f, page: p }))}
        canDelete={isAdmin}
        hasFilters={hasFilters}
      />
    </div>
  );
}
```

- [ ] **Step 3: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores. **Si falla por la clase de Tailwind `bg-accent`**: revisar `globals.css` para confirmar el nombre real de la clase del color amarillo de Reinar (puede ser `bg-yellow` o similar) y reemplazar — el design system está documentado en CLAUDE.md como `--yellow`/`--yellow-dim`. Si las utilidades no existen, usar `bg-yellow text-navy hover:bg-yellow-dim`.

- [ ] **Step 4: Commit**

Run:
```bash
git add "app/(dashboard)/pagos/page.tsx"
git commit -m "feat(pagos): página /pagos con listado, filtros y permisos"
```

---

## Task B9: Crear `components/pagos/SelectorFacturaPendiente.tsx`

**Files:**
- Create: `components/pagos/SelectorFacturaPendiente.tsx`

- [ ] **Step 1: Crear archivo**

Escribir `components/pagos/SelectorFacturaPendiente.tsx` con:

```typescript
'use client';

import { useMemo, useState } from 'react';
import { Icon } from '@/components/ui/Icon';
import { Spinner } from '@/components/ui/Spinner';
import { FacturaEstadoBadge } from '@/components/facturas/FacturaEstadoBadge';
import { formatCurrency } from '@/lib/utils';
import type { FacturaListItem } from '@/types/api';

function nombreCliente(c: FacturaListItem['cliente']): string {
  if (c.tipo === 'EMPRESA') return c.razonSocial ?? '—';
  return [c.nombre, c.apellido].filter(Boolean).join(' ') || '—';
}

type Props = {
  facturas: FacturaListItem[];
  loading: boolean;
  onSelect: (f: FacturaListItem) => void;
};

// Typeahead client-side. Filtramos sobre la lista ya cargada
// (useFacturasPendientes carga 100). El backend no soporta búsqueda por
// numeroFactura ni filtro por múltiples estados; pedirlo aquí abriría un
// PR extra al server por un caso de uso acotado.
export function SelectorFacturaPendiente({ facturas, loading, onSelect }: Props) {
  const [query, setQuery] = useState('');
  const [open, setOpen] = useState(false);

  const resultados = useMemo(() => {
    const base = facturas;
    if (!query) return base.slice(0, 6);
    const q = query.toLowerCase();
    return base
      .filter((f) => {
        const nombre = nombreCliente(f.cliente).toLowerCase();
        return f.numeroFactura.toLowerCase().includes(q) || nombre.includes(q);
      })
      .slice(0, 6);
  }, [facturas, query]);

  return (
    <div className="relative">
      <label className="block text-xs text-tx-3 mb-1">
        Buscar factura pendiente <span className="text-danger">*</span>
      </label>
      <div className="relative">
        <span className="absolute left-3 top-1/2 -translate-y-1/2 text-tx-3 pointer-events-none">
          <Icon name="search" size={14} />
        </span>
        <input
          type="text"
          value={query}
          placeholder="Buscar por número o cliente…"
          onFocus={() => setOpen(true)}
          onBlur={() => setTimeout(() => setOpen(false), 150)}
          onChange={(e) => { setQuery(e.target.value); setOpen(true); }}
          className="w-full pl-9 pr-3 py-2 rounded-md border border-bd bg-bg text-tx placeholder:text-tx-3 focus:outline-none focus:border-accent"
        />
      </div>
      {open && (
        <div className="absolute z-10 left-0 right-0 mt-1 border border-bd rounded-md bg-bg shadow-lg max-h-80 overflow-y-auto">
          {loading ? (
            <div className="flex justify-center py-4"><Spinner /></div>
          ) : resultados.length === 0 ? (
            <div className="px-3 py-4 text-sm text-tx-3 text-center">
              {query
                ? 'No hay facturas con saldo pendiente que coincidan.'
                : 'No hay facturas con saldo pendiente.'}
            </div>
          ) : (
            resultados.map((f) => (
              <button
                key={f.id}
                type="button"
                className="w-full text-left px-3 py-2 hover:bg-bg-sunken border-b border-bd last:border-b-0"
                // onMouseDown en vez de onClick para que dispare antes del
                // onBlur del input (que cierra el dropdown con un setTimeout).
                onMouseDown={() => onSelect(f)}
              >
                <div className="flex items-center justify-between">
                  <span className="font-mono text-sm font-medium text-tx">{f.numeroFactura}</span>
                  <span className="font-mono text-xs font-semibold text-danger">
                    Saldo: {formatCurrency(f.saldoPendiente)}
                  </span>
                </div>
                <div className="text-xs text-tx-3 mt-0.5 flex items-center gap-2">
                  <span>{nombreCliente(f.cliente)}</span>
                  <FacturaEstadoBadge estado={f.estado} />
                </div>
              </button>
            ))
          )}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

Run:
```bash
git add components/pagos/SelectorFacturaPendiente.tsx
git commit -m "feat(pagos): SelectorFacturaPendiente typeahead client-side"
```

---

## Task B10: Crear `components/pagos/FacturaSeleccionadaCard.tsx`

**Files:**
- Create: `components/pagos/FacturaSeleccionadaCard.tsx`

- [ ] **Step 1: Crear archivo**

Escribir `components/pagos/FacturaSeleccionadaCard.tsx` con:

```typescript
'use client';

import Link from 'next/link';
import { Icon } from '@/components/ui/Icon';
import { FacturaEstadoBadge } from '@/components/facturas/FacturaEstadoBadge';
import { formatCurrency } from '@/lib/utils';
import type { FacturaListItem } from '@/types/api';

function nombreCliente(c: FacturaListItem['cliente']): string {
  if (c.tipo === 'EMPRESA') return c.razonSocial ?? '—';
  return [c.nombre, c.apellido].filter(Boolean).join(' ') || '—';
}

type Props = {
  factura: FacturaListItem;
  onClear: () => void;
};

export function FacturaSeleccionadaCard({ factura, onClear }: Props) {
  return (
    <div className="bg-bg-sunken border border-bd rounded-md p-4">
      <div className="flex items-start justify-between gap-4">
        <div>
          <Link
            href={`/facturas/${factura.id}`}
            className="font-mono font-semibold text-tx hover:underline"
          >
            {factura.numeroFactura}
          </Link>
          <div className="text-sm text-tx-2 mt-1">{nombreCliente(factura.cliente)}</div>
          <div className="mt-2"><FacturaEstadoBadge estado={factura.estado} /></div>
        </div>
        <div className="text-right">
          <div className="text-2xs uppercase tracking-wider text-tx-3 font-semibold">Saldo pendiente</div>
          <div className="font-mono text-2xl font-semibold text-danger tabular-nums">
            {formatCurrency(factura.saldoPendiente)}
          </div>
          <div className="font-mono text-xs text-tx-3">
            de {formatCurrency(factura.total)}
          </div>
        </div>
      </div>
      <button
        type="button"
        onClick={onClear}
        className="mt-3 inline-flex items-center gap-1.5 text-xs text-tx-3 hover:text-tx"
      >
        <Icon name="x" size={12} /> Cambiar factura
      </button>
    </div>
  );
}
```

- [ ] **Step 2: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores.

- [ ] **Step 3: Commit**

Run:
```bash
git add components/pagos/FacturaSeleccionadaCard.tsx
git commit -m "feat(pagos): FacturaSeleccionadaCard del flujo de registro"
```

---

## Task B11: Crear `app/(dashboard)/pagos/nuevo/page.tsx`

**Files:**
- Create: `app/(dashboard)/pagos/nuevo/page.tsx`

- [ ] **Step 1: Crear archivo**

Escribir `app/(dashboard)/pagos/nuevo/page.tsx` con:

```typescript
'use client';

import { useEffect, useState } from 'react';
import { useRouter, useSearchParams } from 'next/navigation';
import { PageHeader } from '@/components/ui/PageHeader';
import { FormSection } from '@/components/ui/FormSection';
import { Spinner } from '@/components/ui/Spinner';
import { SelectorFacturaPendiente } from '@/components/pagos/SelectorFacturaPendiente';
import { FacturaSeleccionadaCard } from '@/components/pagos/FacturaSeleccionadaCard';
import { RegistrarPagoForm } from '@/components/pagos/RegistrarPagoForm';
import { useFacturasPendientes } from '@/hooks/use-pagos';
import type { FacturaListItem } from '@/types/api';

export default function NuevoPagoPage() {
  const router = useRouter();
  const searchParams = useSearchParams();
  // Soporta deep-link desde el detalle de factura (?facturaId=xxx) para que
  // el usuario no tenga que volver a buscar lo que ya tenía en pantalla.
  const facturaIdPre = searchParams.get('facturaId');

  const [factura, setFactura] = useState<FacturaListItem | null>(null);
  const { data: pendientes = [], isLoading } = useFacturasPendientes();

  // Pre-selección desde query param: corre una vez cuando termina de
  // cargarse la lista. Si el ID no está entre las elegibles (factura ya
  // pagada o anulada), se ignora y el usuario debe buscar manualmente.
  useEffect(() => {
    if (facturaIdPre && !factura && pendientes.length > 0) {
      const f = pendientes.find((x) => x.id === facturaIdPre);
      if (f) setFactura(f);
    }
  }, [facturaIdPre, pendientes, factura]);

  return (
    <div>
      <PageHeader
        title="Registrar pago"
        subtitle="Aplicá un pago a una factura pendiente. Los pagos no pueden editarse después."
        back
        backLabel="Volver a pagos"
        onBack={() => router.push('/pagos')}
      />

      <FormSection title="Factura">
        {factura ? (
          <FacturaSeleccionadaCard factura={factura} onClear={() => setFactura(null)} />
        ) : isLoading ? (
          <div className="flex justify-center py-6"><Spinner /></div>
        ) : (
          <SelectorFacturaPendiente
            facturas={pendientes}
            loading={false}
            onSelect={setFactura}
          />
        )}
      </FormSection>

      {factura && (
        <FormSection title="Datos del pago">
          <RegistrarPagoForm
            facturaId={factura.id}
            saldoPendiente={factura.saldoPendiente}
            onSuccess={() => router.push('/pagos')}
          />
        </FormSection>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Verificar tipos**

Run: `pnpm tsc --noEmit`
Expected: sin errores. **Si `FormSection` no acepta la prop `title` con ese nombre**, leer `components/ui/FormSection.tsx` y ajustar la prop (puede llamarse `heading` o similar — el patrón actual en otras páginas es referencia).

- [ ] **Step 3: Commit**

Run:
```bash
git add "app/(dashboard)/pagos/nuevo/page.tsx"
git commit -m "feat(pagos): página /pagos/nuevo con typeahead y form standalone"
```

---

## Task B12: Verificación manual end-to-end

**Files:**
- N/A (smoke test del flujo completo)

> Pre-requisito: el PR del backend (Parte A) debe estar mergeado y el servidor con el endpoint `/api/v1/pagos` corriendo en `localhost:3000`.

- [ ] **Step 1: Arrancar el frontend**

Run en otra terminal:
```bash
pnpm dev
```
Expected: server en `http://localhost:3001`.

- [ ] **Step 2: Verificar `/pagos`**

Abrir `http://localhost:3001/pagos` logueado como ADMIN.

Checklist:
- [ ] Listado carga, muestra al menos un pago si la BD tiene datos.
- [ ] Subtítulo muestra `N pagos · $X.XX en esta página`.
- [ ] Click en `numeroFactura` (link) navega a `/facturas/[id]`.
- [ ] Click en fila (fuera del link) expande el `PagoDetallePanel` con notas/referencia.
- [ ] Chips de método filtran (probar `EFECTIVO`, `TRANSFERENCIA`, `ANTICIPO`).
- [ ] Rango de fechas filtra (probar último mes).
- [ ] Selector de cliente carga clientes al abrirlo y filtra.
- [ ] Búsqueda libre por número de factura matchea.
- [ ] Búsqueda libre por nombre de cliente matchea (probar con un cliente conocido).
- [ ] Botón "Limpiar" resetea todos los filtros.
- [ ] Paginación funciona si total > 20.
- [ ] Botón eliminar (trash) visible y funcional como ADMIN.
- [ ] Tras eliminar: toast.success, fila desaparece, navegando a la factura se ve saldo recalculado.
- [ ] En un pago de factura ANULADA: botón eliminar deshabilitado con tooltip.

- [ ] **Step 3: Verificar `/pagos/nuevo`**

Click en "Registrar pago" desde `/pagos` o navegar a `http://localhost:3001/pagos/nuevo`.

Checklist:
- [ ] Typeahead carga facturas pendientes (saldo > 0 y estado en {PENDIENTE, PARCIAL, VENCIDA}).
- [ ] Buscar por número (parcial) muestra coincidencias.
- [ ] Buscar por cliente muestra coincidencias.
- [ ] Click en una factura cierra el typeahead y muestra `FacturaSeleccionadaCard`.
- [ ] `RegistrarPagoForm` aparece con monto pre-cargado al saldo pendiente.
- [ ] Cambiar monto a un valor > saldo dispara la advertencia warn-soft inline.
- [ ] Botón "Cambiar factura" vuelve al typeahead, limpia el form.
- [ ] Registrar pago: toast.success, redirect a `/pagos`, el pago aparece en la lista.
- [ ] Navegar a `/pagos/nuevo?facturaId=<id-existente>`: pre-selecciona esa factura.
- [ ] Navegar a `/pagos/nuevo?facturaId=<id-invalido>`: typeahead vacío, sin error en consola.

- [ ] **Step 4: Verificar permisos**

Logout y login como **VISUALIZADOR**:
- [ ] `/pagos` carga, listado visible, botón "Registrar pago" NO visible.
- [ ] Botones de eliminar NO visibles.

Logout y login como **OPERADOR**:
- [ ] Botón "Registrar pago" visible, `/pagos/nuevo` funciona.
- [ ] Botones de eliminar NO visibles (solo ADMIN).

- [ ] **Step 5: Verificar dark mode y tablet**

- [ ] Activar dark mode desde el panel "Tweaks" del sidebar: ambas páginas se ven coherentes (sin colores hardcoded fuera del design system).
- [ ] Resize a ~768px (tablet): filtros se acomodan, tabla scrollea horizontal, typeahead usable.

- [ ] **Step 6: Cierre — verificación final con lint y types**

Run:
```bash
pnpm tsc --noEmit
pnpm lint
```
Expected: ambos pasan sin errores.

---

## Task B13: PR del frontend

- [ ] **Step 1: Push y crear PR**

Run:
```bash
git push -u origin feat/pagos
gh pr create --title "feat(pagos): módulo de pagos — listado global y registro" --body "$(cat <<'EOF'
## Summary
- Páginas nuevas: `/pagos` (listado global con filtros) y `/pagos/nuevo` (registro standalone con typeahead).
- Reutiliza `RegistrarPagoForm` movido a `components/pagos/` para que sirva tanto al detalle de factura como al flujo standalone.
- Hooks nuevos: `useListadoPagos`, `useFacturasPendientes`. Invalidaciones extendidas en `useCrearPago`/`useEliminarPago`.
- Tipos nuevos: `PagoListItem`, `FiltrosPagos`.
- Consume `GET /api/v1/pagos` agregado en el PR del server (mergeado previamente).

## Test plan
- [ ] `pnpm tsc --noEmit` y `pnpm lint` pasan.
- [ ] `/pagos`: filtros por método (chips), rango de fechas, cliente y búsqueda libre funcionan combinados.
- [ ] Paginación server-side funciona.
- [ ] Click en fila expande detalle con notas/referencia.
- [ ] Eliminar pago como ADMIN refresca el listado y el detalle de la factura asociada.
- [ ] Botón eliminar deshabilitado en pagos de facturas ANULADAS.
- [ ] `/pagos/nuevo`: typeahead filtra correctamente; deep-link `?facturaId=` pre-selecciona.
- [ ] VISUALIZADOR no ve botones de escritura.
- [ ] Dark mode sin regresiones; tablet 768px usable.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

Expected: PR URL.

---

## Self-Review (referencia post-implementación)

1. **Cobertura del spec:** todas las secciones del spec tienen tarea correspondiente.
   - Backend cambios: Task A2-A6.
   - Hooks nuevos: Task B4.
   - Mover form: Task B3.
   - Filtros UI: Task B5.
   - Tabla con expansión y delete: Task B6, B7.
   - Página listado: Task B8.
   - Typeahead + card factura: Task B9, B10.
   - Página nuevo: Task B11.
   - Permisos por rol: cubiertos en B8 y verificados en B12.
   - Manejo de errores con toasts e inline: implementado en B3 (form) y B4 (hooks).
2. **Sin placeholders:** todo el código está incluido inline; no hay TBDs ni "implement later".
3. **Consistencia de tipos:** `PagoListItem.factura.estado: EstadoFactura`, `metodoPago: MetodoPago`, `FiltrosPagos.metodoPago: MetodoPago` — todos coherentes entre tasks.
4. **Comentarios "why":** incluidos en cada lugar donde el "por qué" no es obvio (filter client-side, queryKey separado, ANTICIPO en filtros, sobrepago, deep-link, total de página).
